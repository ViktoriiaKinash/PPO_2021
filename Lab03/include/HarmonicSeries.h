#ifndef HARMONIC_SERIES_H
#define HARMONIC_SERIES_H

#include "Series.h"

SeriesPtr InitializeHarmonicSeries(Size hSize); /* function that takes size of array as an argument and stores the elements into the array using dymanic memory allocation, returns pointer to an array */
#endif